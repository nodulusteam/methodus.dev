{
    "name": "<%= projectName %>",
    "version": "1.0.0",
    "main": "dist/index.js",
    "scripts": {
        "test": "npm run build && jest --detectOpenHandles",
        "build": "rimraf dist && tsc",
        "start": "npm run build  && node dist/index.js",
        "start-build": "ts-node src/index.ts"
    },
    "license": "MIT",
    "dependencies": {
        "@methodus/describe": "^7.0.7",
        "@methodus/platform-express": "^5.0.12",
        "@methodus/platform-rest": "^5.0.8",
        "@methodus/server": "^5.0.23"
    },
    "devDependencies": {
        "@types/node": "^13.9.0",
        "jest": "^25.1.0",
        "ts-jest": "^25.2.1",
        "@types/jest": "^25.1.4",
        "ts-node": "^8.8.1",
        "typescript": "^3.8.3"
    }
}
